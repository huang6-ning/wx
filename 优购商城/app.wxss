/* 导入 style 文件中的icon.wxss 设为全局样式 */
@import './style/icon';

page,view,text,swiper,swiper-item,image,navigator {
    padding: 0;
    margin: 0;
    box-sizing: border-box;
}

page {
    /* 定义主体颜色 */
    --themeColor: #e94141;

    font-size: 28rpx;
}

image {
    width: 100%;
}